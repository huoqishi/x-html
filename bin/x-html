#!/usr/bin/env node

const argHandler = require('./argHandler')
const funHandler = require('./funHandler')
main()
function main () {
  if (!argHandler.validArgs()) {
    return
  }
  console.log('验证通过，开始解析')
  // console.log(funHandler)
  funHandler.start()
}
